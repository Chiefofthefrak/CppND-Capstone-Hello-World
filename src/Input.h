#ifndef INPUT_H
#define INPUT_H

#include <vector>
#include <thread>
#include <mutex>

class Input
{
public:
    // constructor / desctructor
    Input();
    ~Input();

    // getter and setter


    // typical behaviour methods
    void receiveInput();

    // behaviour for specific actions


protected:

    };

#endif